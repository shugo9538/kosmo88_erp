<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.kosmo88.logistics_erp.account.dao.UserDAO">
	<select
		id="getUserData"
		parameterType="String"
		resultType="UserDTO">
		SELECT userid, password, username, tel, advAlert, enabled, authority, key
		FROM USER_TBL
		WHERE userid = #{userid}
	</select>

	<insert
		id="insertUser"
		parameterType="UserDTO">
		INSERT INTO USER_TBL(userid, password, username, tel, key, advAlert, enabled, authority)
		VALUES(#{userid}, #{password}, #{username}, #{tel}, #{key}, 'OK', 0, 'ROLE_GUEST')
	</insert>
</mapper>