<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="StockDao">
<!-- 	namespace="com.kosmo88.logistics_erp.wms.dao.StockDao"> -->



<!-- 	입고시 insert 처리한다 -->
	<insert id="insert"
	parameterType="StockDto">
	INSERT INTO STOCK
	</insert>

	<select id="selectMaxId" resultType="int">
		SELECT MAX(ID) 
		FROM SECTION
	</select>

<!-- stockList -->
		<select id="selectList"
			parameterType="int"
			resultType="com.kosmo88.logistics_erp.wms.dto.V_Stock_SectionDto">
			SELECT * FROM V_STOCK_SECTION
			WHERE WAREHOUSE_ID = #{warehouseId} 
<!-- 			AND ITEM_ID = 1 -->
		</select>
	</mapper>    	
